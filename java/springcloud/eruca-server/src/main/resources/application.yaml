server:
  port: 8761

spring:
  application:
    name: eureka-server

eureka:
  server:
    ip: 127.0.0.1

  client:
    #取消自我注册
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://${eureka.server.ip}:${server.port}/eureka/

